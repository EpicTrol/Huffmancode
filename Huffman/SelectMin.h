/*#ifndef _SelectMin
#define _SelectMin
#include<iostream>
using namespace std;
const int DefaultSize = 30;
template<class T>
class SelectMin {
private:
	T * arr;
	int currentSize;
public:
	SelectMin(int sz = DefaultSize);
	SelectMin(T w[], int n);
	bool Insert(const T&n);
	bool Remove(T& n);

};

#endif*/
